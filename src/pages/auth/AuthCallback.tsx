import { SimplePage } from "@/pages/_shared/SimplePage";

export default function AuthCallback() {
  return (
    <SimplePage title="Auth Callback">
      <p className="text-muted-foreground">Callback de autenticação (UI placeholder).</p>
    </SimplePage>
  );
}
